# Live response fixtures

`cds-contract.json` was captured from CDS on 2026-09-17 and reduced to the fields used by the client. Job IDs, trace IDs, asset URLs, local paths, checksums, and account-specific data are removed or replaced.
